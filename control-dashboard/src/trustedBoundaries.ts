/**
 * Production trust boundary tombstone.
 *
 * Trusted readiness/evidence/authorization issuers live only in
 * ../test-support/trustedP0Harness.ts. Browser production code intentionally
 * receives no issuer or evaluator capability until a trusted backend boundary
 * is connected.
 */
export {};
